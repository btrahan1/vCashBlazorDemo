@page "/"

<PageTitle>vCash Money Center</PageTitle>

<div class="d-flex flex-column align-items-center justify-content-center h-100 text-center">
    <div class="mb-4">
        <h1 class="display-4 fw-bold text-primary">vCash Money Center</h1>
        <p class="lead text-muted">Next Generation Cash Management System</p>
    </div>

    <!-- Recycler Image (Using the generated image placeholder logic since no file path is guaranteed stable in output, assuming relative path for artifact or similar. 
         For this environment, I'll use a placeholder styled div or the image if I could move it. 
         I will use a standard image tag pointing to the artifact location if copied, but best practice here is to instruct user to verify or just use the layout.)
         
         Actually, I will use the image I just generated. I need to copy it to the wwwroot first.
         Wait, I can't copy to wwwroot easily without a command. I will check if I can just reference it or if I should use a cool CSS placeholder first.
         The user said "Put a recycler image on there...".
         I will write the code assuming the image 'bill_recycler.png' exists in 'images' folder, and I will execute the copy command next.
     -->
    <div class="my-4">
        <img src="images/bill_recycler.png" alt="vCash Recycler" class="img-fluid" style="max-height: 400px; filter: drop-shadow(0 0 10px rgba(0,123,255,0.5));" />
    </div>

    <div class="mt-4">
        <a href="vcash/transaction" class="btn btn-primary btn-lg px-5 shadow-sm">
            <i class="bi bi-play-circle-fill me-2"></i> Start Money Center/POS
        </a>
    </div>
</div>
